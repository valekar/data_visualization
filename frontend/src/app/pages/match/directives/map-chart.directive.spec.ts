import { MapChartDirective } from './map-chart.directive';

describe('MapChartDirective', () => {
  it('should create an instance', () => {
    const directive = new MapChartDirective();
    expect(directive).toBeTruthy();
  });
});
