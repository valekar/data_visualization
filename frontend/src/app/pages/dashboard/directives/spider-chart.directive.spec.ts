import { SpiderChartDirective } from './spider-chart.directive';

describe('SpiderChartDirective', () => {
  it('should create an instance', () => {
    const directive = new SpiderChartDirective();
    expect(directive).toBeTruthy();
  });
});
